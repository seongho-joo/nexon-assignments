export enum ServiceNameEnum {
  GATEWAY = 'gateway',
  AUTH = 'auth',
  EVENT = 'event',
}

export enum GatewayCommandEnum {
  'AUTH' = 'auth',
  'EVENT' = 'event',
  'REQUEST' = 'request',
  'POINT_TRANSACTION' = 'point-transaction',
}
